---
title: 'Rate Limits'
description: 'Understanding API rate limits and best practices'
---

## Rate Limit Tiers

<Tabs>
  <Tab title="Standard">
    - **1,000 requests/hour** per API key
    - **10,000 requests/day** per API key
    - Suitable for most applications
  </Tab>

  <Tab title="Professional">
    - **5,000 requests/hour** per API key
    - **50,000 requests/day** per API key
    - For high-volume applications
  </Tab>

  <Tab title="Enterprise">
    - **Custom limits** based on needs
    - Dedicated rate limit pools
    - Contact sales for details
  </Tab>
</Tabs>

## Rate Limit Headers

Every API response includes rate limit information:

```http
X-RateLimit-Limit: 1000
X-RateLimit-Remaining: 847
X-RateLimit-Reset: 1698077400
```

## Handling Rate Limits

```python
from ava import Client
import time

client = Client(api_key="YOUR_API_KEY")

def make_request_with_retry():
    try:
        return client.data_sources.list()
    except client.RateLimitError as e:
        # Wait until rate limit resets
        time.sleep(e.retry_after)
        return make_request_with_retry()
```

## Best Practices

<CardGroup cols={2}>
  <Card title="Implement Backoff" icon="clock">
    Use exponential backoff for retries
  </Card>
  <Card title="Cache Responses" icon="database">
    Cache frequently accessed data
  </Card>
  <Card title="Batch Requests" icon="layer-group">
    Use batch endpoints when available
  </Card>
  <Card title="Monitor Usage" icon="chart-line">
    Track your API usage regularly
  </Card>
</CardGroup>

## Next Steps

<CardGroup cols={2}>
  <Card title="Data Sources API" icon="database" href="/api-reference/endpoints/data-sources">
    Manage data sources via API
  </Card>
  <Card title="SDK Documentation" icon="book-code" href="/sdk/overview">
    Use official SDKs with built-in rate limiting
  </Card>
</CardGroup>
